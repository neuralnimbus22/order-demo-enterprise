apiVersion: apps/v1
kind: Deployment
metadata:
  name: order
  namespace: order-demo
  labels:
    app: order
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order
  template:
    metadata:
      labels:
        app: order
    spec:
      containers:
        - name: order
          image: ghcr.io/neuralnimbus22/order-demo-order:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3002
          env:
            - name: PORT
              value: "3002"
            - name: AUTH_URL
              value: http://auth.order-demo.svc.cluster.local:3001
            - name: AUTH_TOKEN
              value: demo-token-good
            - name: KAFKA_BROKERS
              value: kafka.order-demo.svc.cluster.local:9092
            - name: KAFKA_TOPIC
              value: order-placed
          readinessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 2
            periodSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: order
  namespace: order-demo
  labels:
    app: order
spec:
  type: ClusterIP
  selector:
    app: order
  ports:
    - name: http
      port: 3002
      targetPort: 3002
